/*import 'package:flutter/material.dart';
import 'package:garbage_movil/components/consultar_item_denuncia.dart';
import 'package:garbage_movil/components/consultar_item_evento.dart';
import 'package:garbage_movil/models/denuncia.dart';
import 'package:garbage_movil/models/evento.dart';

// ignore: camel_case_types
class Evento_lista extends StatelessWidget {

  final List<Evento> eventos;

  const Evento_lista(this.eventos, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      shrinkWrap: true,
      physics: const NeverScrollableScrollPhysics(),
      itemCount: eventos.length,
      itemBuilder: (context, index) => Consultar_item_evento(eventos[index]) ,
      
    );
  }
}*/
