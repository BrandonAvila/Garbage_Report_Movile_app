/*import 'package:flutter/material.dart';
import 'package:garbage_movil/components/consultar_item_denuncia.dart';
import 'package:garbage_movil/models/denuncia.dart';

// ignore: camel_case_types
class Denuncia_lista extends StatelessWidget {

  final List<Denuncia> denuncias;

  const Denuncia_lista(this.denuncias, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      shrinkWrap: true,
      physics: const NeverScrollableScrollPhysics(),
      itemCount: denuncias.length,
      itemBuilder: (context, index) => Consultar_item_denuncia(denuncias[index]) ,
      
    );
  }
}*/
